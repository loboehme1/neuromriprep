/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    nf-core/neuromriprep Nextflow config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Default config options for all compute environments
----------------------------------------------------------------------------------------
*/

outputDir = params.outdir ?: 'results'
nextflow.enable.moduleBinaries = true
workflow.output.mode = 'copy'

process {

  /*
   * Container overrides
   * These are generic process-name selectors.
   * They apply wherever the process is used, including inside subworkflows.
   */

  withName: 'DCM2BIDS_CONFIG' {
    ext.container = params.dcm2bids_config_container
  }

  withName: 'DCM2BIDS' {
    ext.container = params.dcm2bids_container
  }

  withName: 'DCM2BIDS_POSTPROC' {
    ext.container = params.dcm2bids_postproc_container
  }

  withName: 'MERGE_BIDS_DATASET' {
    ext.container = params.merge_bids_dataset_container
  }

  withName: 'BIDSIGNORE' {
    ext.container = params.bidsignore_container
  }

  withName: 'BIDS_VALIDATOR' {
    shell = ['sh', '-eu']
    ext.container = params.bidsvalidator_container
  }

  withName: 'BIDS_QC_GATE' {
    ext.container = params.bids_qc_gate_container
  }

  withName: 'MRIQC_PARTICIPANT' {
    ext.container = params.mriqc_participant_container
  }

  withName: 'MRIQC_GROUP' {
    ext.container = params.mriqc_group_container
  }

  withName: 'FMRIPREP' {
    ext.container = params.fmriprep_container
  }

  withName: 'PYDEFACE' {
    ext.container = params.pydeface_container
  }

  withName: 'MRI_DEFACE' {
    ext.container = params.mri_deface_container
  }

  withName: 'FSL_DEFACE' {
    ext.container = params.fsl_deface_container
  }

  withName: 'AFNI_REFACER' {
    ext.container = params.afni_refacer_container
  }

  withName: 'DEFACE_QC_RENDER' {
    ext.container = params.deface_qc_render_container
  }

  withName: 'DEFACE_METRICS' {
    ext.container = params.deface_metrics_container
  }

  withName: 'DEFACE_DETECTOR' {
    ext.container = params.deface_detector_container
  }

  withName: 'MERGE_BENCHMARK' {
    ext.container = params.merge_benchmark_container
  }


  /*
   * Resource / execution settings
   */

  withName: 'NFCORE_NEUROMRIPREP:NEUROMRIPREP:FMRIPREP' {
    memory = '60 GB'
    cpus   = 16
  }

  withName: 'NFCORE_NEUROMRIPREP:NEUROMRIPREP:FMRIPREP_PARTICIPANTS:FMRIPREP' {
    cpus     = 16
    memory   = 60.GB
    maxForks = 3
  }

  withName: 'NFCORE_NEUROMRIPREP:NEUROMRIPREP:MRIQC_PARTICIPANTS:MRIQC_PARTICIPANT' {
    memory   = '30 GB'
    cpus     = 16
    maxForks = 4
  }

  withName: 'NFCORE_NEUROMRIPREP:NEUROMRIPREP:BIDSING:DCM2BIDS' {
    maxForks = 4
  }

  withName: 'NFCORE_NEUROMRIPREP:DEFACE_BENCHMARK:BIDSING:DCM2BIDS' {
    maxForks = 4
  }

  withName: 'NFCORE_NEUROMRIPREP:DEFACE_BENCHMARK:PYDEFACE' {
    maxForks = 4
  }

  withName: 'NFCORE_NEUROMRIPREP:DEFACE_BENCHMARK:AFNI_REFACER' {
    maxForks = 4
  }

  withName: 'NFCORE_NEUROMRIPREP:DEFACE_BENCHMARK:FSL_DEFACE' {
    maxForks = 4
  }

  withName: 'NFCORE_NEUROMRIPREP:DEFACE_BENCHMARK:MRI_DEFACE' {
    maxForks = 4
  }

  withName: 'DEEPDEFACER' {
    ext.container = params.deepdefacer_container
  }
}




// Global default params, used in configs
params {
    // Input options
    input                       = 'assets/samplesheet.csv'
    dcm2bids_config             = null
    
    // Workflow control
    run_complete                = false

    force_dcm2bids              = false

    stop_bidsval                = true
    stop_mriqc                  = true
    stop_fmriprep               = true

    skip_mriqc                  = false
    skip_fmriprep               = false

    // container
    dcm2bids_config_container      = null
    dcm2bids_container             = null
    dcm2bids_postproc_container    = null
    merge_bids_dataset_container   = null
    bidsignore_container           = null
    bidsvalidator_container        = null
    bids_qc_gate_container         = null

    mriqc_participant_container    = null
    mriqc_group_container          = null
    fmriprep_container             = null

    pydeface_container             = null
    mri_deface_container           = null
    fsl_deface_container           = null 
    afni_refacer_container         = null
    deepdefacer_container          = null

    deface_qc_render_container     = null
    deface_metrics_container       = null
    deface_detector_container      = null
    merge_benchmark_container      = null


    //BIDSVALIDATOR options
    enforce_bidsqc_gate         = true


    //MRIQC options
    mriqc_vpn_file              = 'assets/input_pipeline/mriqc_vpn.txt'
    mriqc_group_outdir          = null
    mriqc_part_outdir           = null


    //FMRIPREP options
    fmriprep_vpn_file           = null          // path to vpn list (optional)
    fmriprep_bids_filter        = null          // null | 'ses01' | 'ses02' | '/path/to/filter.json'
    fmriprep_fs_license         = '/nic/sw/FreeSurfer/license.txt'
    fmriprep_outdir             = null


    //PYDEFACE options
    pydeface_vpn_file           = null
    pydeface_outdir             = null


    //MRI_DEFACE options
    mri_deface_bin            = 'mri_deface'
    mri_deface_brain_template = '/opt/mri_deface/talairach_mixed_with_skull.gca'
    mri_deface_face_template  = '/opt/mri_deface/face.gca'

    mri_deface_cpus = 8
    mri_deface_mem  = '8 GB'
    mri_deface_time = '2h'

    deface_tool = 'pydeface'


    // Boilerplate options
    outdir                       = null
    publish_dir_mode             = 'copy'
    email                        = null
    email_on_fail                = null
    plaintext_email              = false
    monochrome_logs              = false
    hook_url                     = System.getenv('HOOK_URL')
    help                         = false
    help_full                    = false
    show_hidden                  = false
    version                      = false
    //pipelines_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/' //test data commented out for proxy
    trace_report_suffix          = new java.util.Date().format( 'yyyy-MM-dd_HH-mm-ss')

    // Config options
    config_profile_name        = null
    config_profile_description = null

    custom_config_version      = 'master'
    //custom_config_base         = "https://raw.githubusercontent.com/nf-core/configs/${params.custom_config_version}"
    config_profile_contact     = null
    config_profile_url         = null

    // Schema validation default options
    validate_params            = true
}

// Load base.config by default for all pipelines
includeConfig 'conf/base.config'

profiles {
    debug {
        dumpHashes              = true
        process.beforeScript    = 'echo $HOSTNAME'
        cleanup                 = false
        nextflow.enable.configProcessNamesValidation = true
    }
    conda {
        conda.enabled           = true
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        conda.channels          = ['conda-forge', 'bioconda']
        apptainer.enabled       = false
    }
    mamba {
        conda.enabled           = true
        conda.useMamba          = true
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    docker {
        docker.enabled          = true
        conda.enabled           = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
        docker.runOptions       = '-u $(id -u):$(id -g)'
    }
    arm64 {
        process.arch            = 'arm64'
        // TODO https://github.com/nf-core/modules/issues/6694
        // For now if you're using arm64 you have to use wave for the sake of the maintainers
        // wave profile
        apptainer.ociAutoPull   = true
        singularity.ociAutoPull = true
        wave.enabled            = true
        wave.freeze             = true
        wave.strategy           = 'conda,container'
    }
    emulate_amd64 {
        docker.runOptions       = '-u $(id -u):$(id -g) --platform=linux/amd64'
    }
    singularity {
        singularity.enabled     = true
        singularity.autoMounts  = true
        conda.enabled           = false
        docker.enabled          = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    podman {
        podman.enabled          = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    shifter {
        shifter.enabled         = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    charliecloud {
        charliecloud.enabled    = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        apptainer.enabled       = false
    }
    apptainer {
        apptainer.enabled    = true
        apptainer.autoMounts = true
    }
    wave {
        apptainer.ociAutoPull   = true
        singularity.ociAutoPull = true
        wave.enabled            = true
        wave.freeze             = true
        wave.strategy           = 'conda,container'
    }
    gpu {
        docker.runOptions       = '-u $(id -u):$(id -g) --gpus all'
        apptainer.runOptions    = '--nv'
        singularity.runOptions  = '--nv'
    }
    test      { includeConfig 'conf/test.config'      }
    test_full { includeConfig 'conf/test_full.config' }
}


// If params.custom_config_base is set AND either the NXF_OFFLINE environment variable is not set or params.custom_config_base is a local path, the nfcore_custom.config file from the specified base path is included.
// Load nf-core/neuromriprep custom profiles from different institutions.
//includeConfig params.custom_config_base && (!System.getenv('NXF_OFFLINE') || !params.custom_config_base.startsWith('http')) ? "${params.custom_config_base}/nfcore_custom.config" : "/dev/null"


// Load nf-core/neuromriprep custom profiles from different institutions.
// TODO nf-core: Optionally, you can add a pipeline-specific nf-core config at https://github.com/nf-core/configs
// includeConfig params.custom_config_base && (!System.getenv('NXF_OFFLINE') || !params.custom_config_base.startsWith('http')) ? "${params.custom_config_base}/pipeline/neuromriprep.config" : "/dev/null"

// Set default registry for Apptainer, Docker, Podman, Charliecloud and Singularity independent of -profile
// Will not be used unless Apptainer / Docker / Podman / Charliecloud / Singularity are enabled
// Set to your registry if you have a mirror of containers
apptainer.registry    = 'quay.io'
docker.registry       = 'quay.io'
podman.registry       = 'quay.io'
singularity.registry  = 'quay.io'
charliecloud.registry = 'quay.io'

// Load igenomes.config if required
//includeConfig !params.igenomes_ignore ? 'conf/igenomes.config' : 'conf/igenomes_ignored.config'

// Export these variables to prevent local Python/R libraries from conflicting with those in the container
// The JULIA depot path has been adjusted to a fixed path `/usr/local/share/julia` that needs to be used for packages in the container.
// See https://apeltzer.github.io/post/03-julia-lang-nextflow/ for details on that. Once we have a common agreement on where to keep Julia packages, this is adjustable.

env {
    PYTHONNOUSERSITE = 1
    R_PROFILE_USER   = "/.Rprofile"
    R_ENVIRON_USER   = "/.Renviron"
    JULIA_DEPOT_PATH = "/usr/local/share/julia"
}

// Set bash options
process.shell = [
    "bash",
    "-C",         // No clobber - prevent output redirection from overwriting files.
    "-e",         // Exit if a tool returns a non-zero status/exit code
    "-u",         // Treat unset variables and parameters as an error
    "-o",         // Returns the status of the last command to exit..
    "pipefail"    //   ..with a non-zero status or zero if all successfully execute
]

// Disable process selector warnings by default. Use debug profile to enable warnings.
nextflow.enable.configProcessNamesValidation = false

// container issues - therefor set to false

timeline {
    enabled = false
    file    = "${params.outdir}/pipeline_info/execution_timeline_${params.trace_report_suffix}.html"
}
report {
    enabled = false
    file    = "${params.outdir}/pipeline_info/execution_report_${params.trace_report_suffix}.html"
}
trace {
    enabled = false
    file    = "${params.outdir}/pipeline_info/execution_trace_${params.trace_report_suffix}.txt"
}
dag {
    enabled = false
    file    = "${params.outdir}/pipeline_info/pipeline_dag_${params.trace_report_suffix}.html"
}

manifest {
    name            = 'nf-core/neuromriprep'
    contributors    = [
        // TODO nf-core: Update the field with the details of the contributors to your pipeline. New with Nextflow version 24.10.0
        [
            name: 'luis',
            affiliation: '',
            email: '',
            github: '',
            contribution: ['contributor'], // List of contribution types ('author', 'maintainer' or 'contributor')
            orcid: ''
        ],
        [
            name: ' mahnaz',
            affiliation: '',
            email: '',
            github: '',
            contribution: ['contributor'], // List of contribution types ('author', 'maintainer' or 'contributor')
            orcid: ''
        ],
        [
            name: 'Carolin Schwitalla',
            affiliation: 'QBiC Quantitative Biology Center, University of Tübingen, Germany',
            email: '',
            github: '@CaroAMN',
            contribution: ['contributor'], // List of contribution types ('author', 'maintainer' or 'contributor')
            orcid: ''
        ],
        [
            name: 'Lorena Boehme',
            affiliation: 'QBiC Quantitative Biology Center, University of Tübingen, Germany',
            email: '',
            github: '@loboehme1',
            contribution: ['contributor'], // List of contribution types ('author', 'maintainer' or 'contributor')
            orcid: ''
        ],
    ]
    //homePage        = 'https://github.com/qbic-pipelines/neuromriprep'
    description     = """preprocessing neuro mri images"""
    mainScript      = 'main.nf'
    defaultBranch   = 'master'
    nextflowVersion = '!>=25.04.0'
    version         = '1.0.0dev'
    doi             = ''
}

// Nextflow plugins
//plugins {
//    id 'nf-schema@2.5.1' // Validation of pipeline parameters and creation of an input channel from a sample sheet
//}

//validation {
//    defaultIgnoreParams = ["genomes"]
//    monochromeLogs = params.monochrome_logs
//}

// Load modules.config for DSL2 module specific options
includeConfig 'conf/modules.config'
